# <单号> <一句话>（关键词 `<KEYWORD>`）

> 通用骨架。项目特有红线覆盖 `$FLEET_HOME/projects/<id>/任务书.md`。派单前复制到 `$FLEET_HOME/` 再改，别在模板上改。

## ★ 硬边界（逐字带）

- 先读项目规程（本机 `AGENTS.md` 或远程规则，以项目任务书为准）。
- **不动生产 / 正式实例**：未获本次明示授权不部署、不重启、不删数据。
- 本单**可写范围**：`<绝对路径全名>`
- 本单**可写数据库**：`<库名或「无」>`
- 舰长本窗不写业务代码。检查点不停手。命中禁区落 BLOCKED。

## 背景与目标

- 事实依据：
- 本单要达成：
- 明确**不做**：

## 要做的

1. … → 验证：
2. … → 验证：

## ★ 判据与反例

- 判据：
- **反例**：
- 正例锚：

## ★ 上下文直供

- **读取白名单**（≤5）：
- **验证命令唯一化**：

## 终态

- 回执落 `$FLEET_HOME/回执/<单号>.md`，首屏三态。落盘即通知。目标时长：。按一次执行。
