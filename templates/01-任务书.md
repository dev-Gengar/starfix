# <单号> <一句话说清这单要什么>（关键词 `<KEYWORD>`）

**语言 / Language：** [中文](01-任务书.md) · [English](01-任务书.en.md)

> 模板用法：`<…>` 是必填占位；带 ★ 的段落是**不许省略**的，它们各自对应一类翻过车的教训。
> 派单前把本文件复制一份改写，别在模板上直接改。
> 本机当前项目的红线骨架在 `$FLEET_HOME/projects/<id>/任务书.md`（`fleet project show`）。新项目怎么登记见 `specs/fleet-projects.md`。

## ★ 硬边界（逐字带，不许省略）

派单方每次都要重写这一段。写在规程里不够——承建方不一定会去读规程，任务书是它一定会读的东西。

- 先读 `<规程文件路径>`，红线以它为准。
- **不动 `<生产系统标识>`**：`<写死禁用的工具/命令清单>`，只许读。
- 本单**可写范围**：`<列绝对路径全名，禁用 views/* 这类缩写>`
- 本单**可写数据库**：`<写死库名，或"无">`
- 命中禁区**停下上报**，不许自行判断"影响很小"。

## 背景与目标

- 事实依据：`<哪份回执/哪次实测得出的结论，给出处>`
- 本单要达成：`<可判定的终态，不是"优化一下">`
- 明确**不做**：`<划走的范围，防止承建方顺手扩围>`

## 要做的

1. `<步骤>` → 验证：`<这一步怎么算过>`
2. `<步骤>` → 验证：`<这一步怎么算过>`

## ★ 判据与反例（没有反例的判据不算判据）

- 判据：`<什么情况下判定通过>`
- **反例**：`<把被测的那半条判据拿掉，它必须红，而且红在该红的那条上>`
- 正例锚：`<一个不受本次改动影响、且实测为"好"的对照点——没有锚的"红"证明不了任何事>`

## ★ 上下文直供（省 token，也防跑偏）

把承建方要用的东西直接贴进来，别让它自己找：

- 相关表结构 / 接口签名：`<贴出来>`
- 目标代码位置：`<文件:行号>`
- 报错原文：`<原样贴，不要转述>`
- **读取白名单**：只许读 `<≤5 个绝对路径>`；除此之外不读文档、不全仓搜索。
- **验证命令唯一化**：`<一条命令 + 期望输出>`，不要让承建方自己发明验证路径。

## 终态

- 回执落 `<回执绝对路径>`，PASS / FAIL / BLOCKED 三态皆落盘。
- 交付前跑 `<收尾检查命令>`，未追踪残留逐个交代。
- 落盘后 `<通知方式>` 报派单方。

## 红线（本单适用）

- 不 push、不部署、不合并主干。
- 一次性资源用完必销毁，并**用清单回查**证明销毁了（不是只看一个计数）。
- 凭据不上命令行、不进回执、不落文件。
