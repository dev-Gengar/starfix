# <task ID> <one line: what this task must deliver> (keyword `<KEYWORD>`)

**语言 / Language:** [中文](01-任务书.md) · [English](01-任务书.en.md)

> How to use this template: `<…>` is a placeholder you must fill in; sections marked ★ **may not be dropped** — each of them corresponds to a class of lesson learned from a real wreck.
> Copy this file and rewrite the copy before dispatching; do not edit the template itself.
> The current project's red-line skeleton lives at `$FLEET_HOME/projects/<id>/任务书.md` (`fleet project show`). How to register a new project: `specs/fleet-projects.md`.

## ★ Hard boundaries (carry verbatim, never drop)

The dispatcher rewrites this section every single time. Putting it in the operating rules is not enough — the builder may not go and read the operating rules, but the task book is the one thing it will certainly read.

- Read `<path to the operating-rules file>` first; it is the authority on the red lines.
- **Do not touch `<production system identifier>`**: `<hard-coded list of forbidden tools/commands>`, read only.
- **Writable scope** for this task: `<list full absolute paths; abbreviations like views/* are forbidden>`
- **Writable database** for this task: `<hard-code the database name, or "none">`
- Hitting a forbidden zone means **stop and report**; never judge for yourself that "the impact is small".

## Background and goal

- Factual basis: `<which receipt / which measurement this conclusion came from; cite the source>`
- What this task must achieve: `<a decidable terminal state, not "make it a bit better">`
- Explicitly **not doing**: `<the scope carved out, so the builder does not widen it on the way>`

## What to do

1. `<step>` → verify: `<what counts as passing this step>`
2. `<step>` → verify: `<what counts as passing this step>`

## ★ Criteria and counterexamples (a criterion without a counterexample is not a criterion)

- Criterion: `<under what condition it is judged to pass>`
- **Counterexample**: `<remove the half of the criterion under test; it must go red, and red on the line that should go red>`
- Positive anchor: `<a control point untouched by this change and measured as "good" — a "red" without an anchor proves nothing>`

## ★ Context supplied directly (saves tokens, and keeps it from wandering off)

Paste in what the builder needs; do not make it go looking:

- Relevant table schemas / interface signatures: `<paste them>`
- Target code location: `<file:line>`
- Error text: `<paste verbatim, do not paraphrase>`
- **Read whitelist**: may read only `<≤5 absolute paths>`; beyond that, no reading docs and no repo-wide search.
- **One and only one verification command**: `<one command + expected output>`; do not let the builder invent its own verification path.

## Terminal state

- The receipt lands at `<absolute receipt path>`; PASS / FAIL / BLOCKED all land on disk.
- Run `<closing check command>` before delivery, and account for every untracked leftover one by one.
- Once it is on disk, report to the dispatcher via `<notification channel>`.

## Red lines (applying to this task)

- No push, no deploy, no merge to trunk.
- Throwaway resources must be destroyed after use, and the destruction must be **proved by a list read-back** (not by looking at a single count).
- Credentials never go on the command line, never go into the receipt, never land in a file.
